server:
  port: 8082
  servlet:
    context-path: /node2

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://119.23.47.158:33007/spring_batch
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
#      naming:
#        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

  # 容器启动是否执行所有的job
  batch:
    job:
      enabled: false



# 每次批量处理的数据量，默认为5000
#batch-size: 5000